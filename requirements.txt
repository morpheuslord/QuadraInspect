# QuadraInspect is managed with uv; see pyproject.toml / uv.lock.
# This file is a pip fallback for users who cannot use uv:
#     pip install -r requirements.txt
#
# It lists the framework's core runtime dependencies plus the extra libraries
# used by the bundled analysis scripts (the [tools] optional extra).

# Core framework
rich>=13.3.3
pyfiglet>=0.7
GitPython>=3.1.31

# Bundled analysis-script helpers ([tools] extra)
pyaxmlparser>=0.3.28
defusedxml>=0.7.1
lxml>=4.6.2
requests>=2.25.1
bs4>=0.0.1
rsa>=4.7
macholib>=1.14
colorlog>=4.7.2
